{
  "type": "object",
  "properties": {
    "_id": {
      "type": "string",
      "description": "Unique ID generated by DB"
    },
    "name": {
      "type": "string",
      "description": "The unique name of the Parameter"
    },
    "units": {
      "type": "string",
      "description": "Parameter's units of measure"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "In date format (ISO8601). Automatically added whenever a Parameter is created."
    }
  }
}
